
  <div class="article" id="register_form">
    {#<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">#}
    {{ form_start(form) }}
      <div class="top">
        <img class="logo" src="{{ asset('img/register.png') }}"/>
        {{ form_widget(form) }}
      </div>
      <div class="bottom">
        <div>
          <a href="/"><p>Déjà un compte ?</p></a>
          <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
        </div>
      </div>
    {{ form_end(form) }}
  </div>
